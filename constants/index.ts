import { COLORS, FONT, SIZES, SHADOWS, BORDER_RADIUS } from './theme'
import icons from './icons'
import sounds from './sounds'
import SVGS from './svgs'
import gifs from './gifs'

export {
    COLORS,
    FONT,
    SIZES,
    SHADOWS,
    BORDER_RADIUS,
    icons,
    sounds,
    SVGS,
    gifs,
}
